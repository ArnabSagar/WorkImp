import pyautogui
import time #for timer


tabbedTimer = true #timer while not studying
seconds = 0
minutes = 0
while tabbedTimer: #Generic timer
	seconds++
	time.sleep(1)
	if seconds == 60:
		Sec = 0
		Min += 1
	if minutes > 0: #If they are tabbed for 1 minute or more
		invertMouse = true
	if minutes = 5:
		toFirstTab = true


while invertMouse:
	currentMouseX, currentMouseY = pyautogui.position()
	pyautogui.moveTo

def toFirstTab(): #This returns you to your first tab on chrome
	pyautogui.hotkey('ctrl', '1')
	exit();

timesCaught = 0

	alert(text='So it looks like someone is slacking off again. Get back to work', title='Really? Again?', button='I'm Sorry')
	timesCaught += 1;